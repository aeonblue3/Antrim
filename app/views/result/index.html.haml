%h2 Results for Closed Ballots

%div
	-@ballots.each do |ballot|
		%p.ballot
			%a{href: "result/#{ballot.id}"}
				=ballot.name
			%span
				Expired 
				=distance_of_time_in_words_to_now ballot.end 
				ago